[build]
builder = "DOCKERFILE"
watchPatterns = ["/apps/api/**", "/railway.toml"]

[deploy]
startCommand = "npm run start:deploy"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
